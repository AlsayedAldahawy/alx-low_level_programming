#include "main.h"
#include <stdio.h>

/**
 * mul -  a function that multiplies two integers.
 * @a: first number.
 * @b: second number.
 * Return: the result of multiblication.
 */

int mul(int a, int b)
{
	return (a * b);
}
