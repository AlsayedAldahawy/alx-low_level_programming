#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @x: 1st argument value
 * @y: 2nd argument value
 * Return: returns the addition value of the two arguments
 */
int add(int x, int y)
{
	return (x + y);
}
